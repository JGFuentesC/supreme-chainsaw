fastapi==0.111.0
uvicorn==0.29.0
google-generativeai==0.7.1
python-dotenv==1.0.1
requests==2.32.3
google-cloud-bigquery==3.25.0
jinja2==3.1.4
aiofiles==23.2.1
# Assuming 'python-adk' is the correct package name as per the prompt.
# If it's available on PyPI, it will be installed from there.
# If it's a private or local package, you'll need to handle its installation separately. 